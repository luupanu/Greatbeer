<h2><%= @brewery.name %></h2>

<p>
  <i>Established in <%= @brewery.year %></i>
</p>

<p>
  Number of beers: <%= @brewery.beers.count%>
</p>

<p>
  <% @brewery.beers.each do |beer| %>
    <%= link_to beer.name, beer %>
  <% end %>
</p>

<p>
  <% if @brewery.average_rating %>
    Average rating: <%= @brewery.average_rating %> <i>(Rated <%= pluralize(@brewery.ratings.count, 'time') %>)</i>
  <% else %>
    <i>This brewery has no ratings yet.</i>
  <% end %>
</p>

<% if current_user %>
  <%= link_to 'Edit', edit_brewery_path(@brewery) %> |
  <%= link_to 'Delete', @brewery, method: :delete, data: { confirm: 'Are you sure?' } %> |
<% end %>
<%= link_to 'Back', breweries_path %>
