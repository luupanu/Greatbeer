<h2>Create new rating</h2>

<%= form_for(@rating) do |form| %>
  <%= form.select :beer_id, options_from_collection_for_select(@beers, :id, :to_s) %>
  score: <%= form.number_field :score %>
  <%= form.submit %>
<% end %>